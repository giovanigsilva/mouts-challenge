apiVersion: 1

providers:
  - name: DeveloperStore
    orgId: 1
    folder: DeveloperStore
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
